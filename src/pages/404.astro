---
// @ts-nocheck

import HtmlLayout from '@/layouts/HtmlLayout.astro';
import MainLayout from '@/layouts/MainLayout.vue';
import Button from '@/components/button/Button.vue';
import ContentsInnerLayout from '@/layouts/ContentsInnerLayout.vue';

const descStyle = 'text-lg text-gray-600 break-keep max-md:text-base';
---

<HtmlLayout bodyClassName="bg-sky-50" robots="noindex, nofollow">
	<MainLayout type="404" client:load>
		<ContentsInnerLayout client:load type="404">
			<div class="text-center">
				<p class="text-8xl text-gray-900 font-bold max-md:text-7xl">404</p>
				<p class="text-3xl text-gray-900 font-semibold mt-1 max-md:text-xl">
					페이지를 찾을 수 없습니다.
				</p>
			</div>

			<div class="text-center mt-8 max-md:mt-6">
				<p class={descStyle}>
					존재하지 않는 페이지이거나, 주소가 변경 또는 삭제되었을 수도 있습니다.
				</p>
				<p class={descStyle}>페이지 주소가 정확한지 다시 한번 확인해주세요.</p>
			</div>
			<div class="mt-4">
				<Button
					effectType="3d"
					bindClass="mx-auto"
					styleType="fill-blue1-md"
					clickType="homeRedirect"
					client:load>홈으로 이동</Button
				>
			</div>
		</ContentsInnerLayout>
	</MainLayout>
</HtmlLayout>
